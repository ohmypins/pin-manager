export const PIN_PORT = process.env.PORT ?? 3000;
export const PIN_HOST = process.env.PIN_HOST ?? 'localhost';
export const PIN_NAME = process.env.PIN_NAME ?? 'pin-manager';
export const PIN_VERSION = process.env.PIN_VERSION ?? '0.0.0 (default)';
export const PIN_TAG = `${PIN_NAME} ${PIN_VERSION}`;
